# 主页访问统计

页脚 `#visitor-stats` 展示今日访客、今日浏览和累计浏览。接入时间：2026-09-07。历史访问不能补录。

- 后端：qiushaocloud/site-counter 的公共服务。客户端仅请求汇总计数，不加载远程 JavaScript。
- 统计范围固定为 `zhouchengjian-user.github.io/personal-homepage/`；`index.html` 合并，查询参数和页内锚点不发送、不单独计数。其他域名和路径不计数。
- UV 按浏览器本地存储的北京时间日期去重，PV 每次页面加载计数一次。同一用户跨设备、清除存储或浏览器限制等可能影响准确性；不是实名人数。
- 使用 Web Locks 避免同一浏览器多个标签同时首次加载而重复计 UV。
- 每日数由服务端累计数减去昨日快照计算，以服务端日切为准；客户端按 Asia/Shanghai 去重。累计浏览从接入后开始累计。
- 服务会接收请求 IP、User-Agent、固定主页地址和计数数据。客户端不发送查询参数、锚点、姓名、邮箱或聊天内容，不携带第三方 Cookie。该公共服务可能保留访问日志；不适合有严格私有分析或数据留存保证需求的网站。
- 请求超时或返回异常时保留 `—`，展示暂不可用，不伪造 0，不自动重试，以免重复累计。统计故障不会阻塞主页。
- 第三方公共服务没有本站可控制的可用性和数据保留承诺。若需长期报表或可靠数据保留，应迁移至专用分析后台。

关闭统计：移除 `index.html` 中的 `visitor-stats.js` 引用。公开客户端签名来自上游协议，不是用户凭证。

上游文档：https://github.com/qiushaocloud/site-counter
