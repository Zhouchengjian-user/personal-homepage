# 小周 · 主页数字助手

小尺寸机器人与原主页并存。原有正文、照片、视频、`style.css`、`script.js` 和三套主题保持不变。

## 当前实现

点击「我是数字人小周，想聊聊吗？」打开通话面板并请求麦克风。配置好语音后端后，小周主动欢迎，使用百炼北京的 `qwen3.5-omni-flash-realtime` 和与小天相同的 `Ethan` 男声，支持连续对话、用户插话、按钮打断、静音和挂断。嘴部开合跟随实际 PCM 播放时钟与能量，不是音素级口型识别。

开场白：

> 你好，我是小周，承健的数字人助手。想了解他的经历、项目和产品思考，直接和我聊聊吧。

语音回复以简历与主页资料为边界，区分过往项目成果与当前演示能力。页面显示语音转写；关闭面板、离开页面或切换到文字模式时，释放麦克风、停止播放并断开连接。

「不知道聊什么？」提供可直接说出口的问题提示。「暂不开麦，打字聊」切换为明确标注的本地资料问答，不调用浏览器云语音识别，也不冒充生成式模型响应。

## 接入

GitHub Pages 只能提供静态文件，实时语音需独立 HTTPS/WSS 后端。`xiaozhou-config.mjs` 只填写公开的 `realtimeEndpoint`，如 `wss://自己的语音服务域名/api/realtime`，不存放模型 API Key。

本地配套服务位于相邻目录 `../xiaozhou-service`，默认监听 `127.0.0.1:5183`。打开 `http://127.0.0.1:5183/` 可预览。服务端凭证从私有环境变量或仅本机可访问的配置页读取；它们不能放入此静态目录、GitHub 或部署前端包。

本地网页会连接本机 5183 端口。公网 `realtimeEndpoint` 留空时，不会尝试把访客连接到开发者的本机；界面会提示语音服务未就绪并提供文字备用。

## 文件

- `xiaozhou.mjs`：主页入口、通话 UI、字幕和本地文字问答。
- `xiaozhou.css`：仅作用于 `xz-` 组件的主题样式。
- `xiaozhou-realtime.mjs`：麦克风、WebSocket、PCM 播放、打断与嘴部能量。
- `xiaozhou-audio-worklet.js`：麦克风转换为 16 kHz PCM。
- `xiaozhou-knowledge.mjs`：公开资料和本地问答；导出后端所用的知识文本。
- `xiaozhou-config.mjs`：公开语音后端地址。
- `xiaozhou-robot.png`：机器人底图，脸部用 SVG 表达会话状态。

传输层与界面已通过离线测试；另外使用合成麦克风连接真实百炼服务，验证了Ethan开场、连续提问、语音打断、音频驱动嘴型和资源释放。真人麦克风、外放回声与部署后的公网网络表现仍需分别验收。
