# 小周 · 主页数字助手

小尺寸机器人与原主页并存。此次接入保留主分支最新的正文、照片、视频、访问统计、`style.css`、`script.js` 和三套主题，新增样式仅作用于小周组件。

## 当前实现

点击「我是数字人小周，想聊聊吗？」打开通话面板并请求麦克风。连接语音后端后，小周主动欢迎，使用百炼北京的 `qwen3.5-omni-flash-realtime` 和 `Ethan` 男声，支持连续对话、用户插话、按钮打断、静音和挂断。机器人发光嘴部的开合跟随实际 PCM 播放时钟与能量，不是音素级口型识别，也不代表三维人物渲染。

开场白：

> 你好，我是小周，承健的数字人助手。想了解他的经历、项目和产品思考，直接和我聊聊吧。

语音回复以简历与主页资料为边界，区分过往项目成果与当前演示能力。页面显示语音转写；关闭面板、离开页面或切换到文字模式时，释放麦克风、停止播放并断开连接。

「不知道聊什么？」提供可直接说出口的问题提示。「暂不开麦，打字聊」切换为明确标注的本地资料问答，不调用浏览器云语音识别，也不冒充生成式模型响应。

## 部署与配置

前端由 GitHub Pages 提供，语音后端部署在火山引擎北京，通过 WSS 代理连接阿里云百炼北京。网页麦克风发送 16 kHz PCM，浏览器接收并播放 24 kHz PCM；模型凭证只配置在后端环境变量中。此仓库仅包含静态前端和公开资料，不包含百炼 API Key、云平台凭证或完整私有简历文件。

`xiaozhou-config.mjs` 只填写公开的 `realtimeEndpoint`，格式为 `wss://自己的语音服务域名/api/realtime`。后端需允许精确来源 `https://zhouchengjian-user.github.io`，不能把带 `/personal-homepage/` 的页面路径当作 Origin。前端模块、机器人图片和 AudioWorklet 使用相对 URL，可部署在 GitHub Pages 项目子路径。

开发环境可另行启动配套本地服务，默认监听 `127.0.0.1:5183`，打开 `http://127.0.0.1:5183/` 预览。后端源码及运行配置不由此静态仓库提供。

本地网页会连接本机 5183 端口。公网 `realtimeEndpoint` 留空时，不会尝试把访客连接到开发者的本机；界面会提示语音服务未就绪并提供文字备用。

## 用量与异常处理

配套云端服务默认单次通话最长 5 分钟，全站每日共享最多 30 分钟，最多同时 2 路通话，同一 IP 每小时最多发起 6 次。并发和每日额度使用火山 TOS 共享状态校验，不依赖单个函数实例的临时文件。时长额度用于控制公开演示的用量，不是 Token 账单；实际模型费用按百炼用量计算，火山托管及存储费用另计。

达到单次时长上限会提示并结束通话；全站额度耗尽、连接过于频繁或并发已满时，后端拒绝新连接。麦克风权限、网络或语音服务发生异常时，界面提供重试与文字备用入口。关闭面板、切换文字模式、隐藏或离开页面均释放音频资源。

## 文件

- `xiaozhou.mjs`：主页入口、通话 UI、字幕和本地文字问答。
- `xiaozhou.css`：仅作用于 `xz-` 组件的主题样式。
- `xiaozhou-realtime.mjs`：麦克风、WebSocket、PCM 播放、打断与嘴部能量。
- `xiaozhou-audio-worklet.js`：麦克风转换为 16 kHz PCM。
- `xiaozhou-knowledge.mjs`：公开资料和本地问答；导出后端所用的知识文本。
- `xiaozhou-config.mjs`：公开语音后端地址。
- `xiaozhou-robot.png`：机器人底图，脸部用 SVG 表达会话状态。

## 验证范围

传输层与界面已通过离线测试；另已在本地代理上使用合成麦克风连接真实百炼服务，验证 Ethan 开场、连续提问、语音打断、音频驱动嘴部和资源释放。这不等同于火山生产环境验收。云端共享额度、真实云端音频、GitHub Pages 发布后的访问，以及真人麦克风与外放回声表现需分别验证；尚未完成的项目不能据此宣称通过。
