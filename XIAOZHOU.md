# 小周 · 主页数字助手

以独立组件接入现有主页。原有 `style.css`、`script.js`、正文、照片、视频和三套主题保持不变；`index.html` 仅增加组件的样式与模块引用。

## 当前可用

- 首屏小尺寸机器人、轻微待机动画、会话状态和播报时的嘴部动画。
- 点击「和小周聊聊」打开对话；支持问题提示、输入、追问、资料来源、重新开始。
- 根据简历与主页的公开内容做本地规则问答。结果数据明确标注来自简历；资料没有的内容不补造。
- 浏览器支持时，可主动开启语音输入、朗读回答和停止播报。语音识别服务及声音由浏览器/系统决定。

当前未接入生成式模型，也不是持续双向语音通话。嘴部动画表达正在播报的状态，不是音素级口型同步。上述实时能力需后续语音接口支持。

## 本地运行

```sh
python3 -m http.server 5182 --bind 127.0.0.1
```

打开 `http://127.0.0.1:5182/`。通过 HTTP 服务运行，避免直接打开文件造成模块加载失败。

## 后续接入模型

GitHub Pages 承载静态网页。模型密钥放在独立后端的环境变量中，不能写入网页、配置文件或提交到仓库。

在 `xiaozhou-config.mjs` 的 `endpoint` 填写后端 HTTPS 地址。组件发送：

```json
{"question":"介绍一下数字人项目","history":[{"role":"user","content":"介绍一下周承健"}]}
```

后端返回：

```json
{"text":"依据简历，……","sources":[{"label":"主页 · 项目","url":"#projects"}],"topic":"avatar"}
```

`history` 最多传递最近 10 条消息，可能含有用于追问的 `topic`。后端应独立校验输入、限制频率和费用，并将本目录 `xiaozhou-knowledge.mjs` 导出的 `knowledgeText` 作为资料上下文。只允许本主页来源访问。前端支持取消请求和超时提示；取消浏览器请求并不保证上游模型已停止计费，后端也应转发取消信号。

如接入实时语音，需要额外实现音频流、播放队列、打断取消与真实音频驱动动画；当前 JSON 接口只负责文字问答。

## 文件

- `xiaozhou.mjs`：对话组件、状态和语音增强。
- `xiaozhou.css`：限定在 `xz-` 组件内的样式，继承主页主题。
- `xiaozhou-knowledge.mjs`：经整理的公开知识和本地问答。
- `xiaozhou-config.mjs`：公开后端地址，不存放密钥。
- `xiaozhou-robot.png`：依据用户机器人参考图生成的透明背景角色，脸部显示由 SVG 动画叠加。

不需要该组件时，移除 `index.html` 内两条 `xiaozhou` 引用即可恢复原主页。
